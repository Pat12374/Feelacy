import Link from "next/link";
import type { CategoryGroup } from "@/lib/category-groups";

export function CategoryLanding({ slug, title, description, groups }: { slug: string; title: string; description: string; groups: readonly CategoryGroup[] }) {
  return <div className="wt-container py-10"><p className="text-xs font-semibold uppercase tracking-[.18em] text-[var(--copper)]">Marketplace category</p><h1 className="mt-3 font-display text-5xl tracking-tight sm:text-6xl">{title}</h1><p className="mt-4 max-w-2xl text-[var(--ink-soft)]">{description}</p><div className="mt-10 grid grid-cols-3 gap-2 sm:gap-4 lg:gap-6">{groups.map((group,index)=><Link key={group.slug} href={`/categories/${slug}/${group.slug}`} className={`flex min-h-44 flex-col justify-between rounded-2xl border border-[var(--line)] p-3 transition hover:-translate-y-1 hover:shadow-lg sm:min-h-60 sm:p-6 lg:min-h-72 lg:p-7 ${index===0?"bg-[rgba(201,168,122,.16)]":index===1?"bg-[rgba(111,35,47,.09)]":"bg-[rgba(31,61,50,.07)]"}`}><div><p className="hidden text-xs uppercase tracking-[.16em] text-[var(--copper)] sm:block">Collection</p><h2 className="font-display text-lg leading-tight sm:mt-3 sm:text-2xl lg:text-3xl">{group.title}</h2><p className="mt-2 hidden text-sm leading-6 text-[var(--ink-soft)] sm:block">{group.description}</p></div><span className="mt-4 text-xs font-semibold text-[var(--bottle)] sm:text-sm">Explore →</span></Link>)}</div><Link href={`/search?category=${slug}`} className="wt-btn wt-btn-primary mt-8">View all available {title.toLowerCase()}</Link></div>;
}
